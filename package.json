{
  "name": "api",
  "version": "1.0.0",
  "description": "Example of a secure serverless API with basic CRUD functionality that can be accessed from third party domains. JWT token verification is implemented via jose library and middlewares.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/edge": "^1.1.0",
    "dotenv": "^16.0.3",
    "jose": "^5.1.0"
  },
  "engines": {
    "node": "20.x"
  }
}
